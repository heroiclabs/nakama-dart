name: flutter_nakama
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  grpc: ^3.0.0
  protobuf: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  faker: ^2.0.0
  pedantic: ^1.11.0
